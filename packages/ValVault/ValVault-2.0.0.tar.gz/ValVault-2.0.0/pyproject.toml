[project]
name = "ValVault"
version = "2.0.0"
readme = "README.md"
authors = [{ name = "PWall" }]
license = {text = "GPL-3.0"}
keywords = ["valorant", "vault", "keepass"]
dependencies = [
    "requests",
    "pykeepass",
    "jwt",
    "hachoir",
]
requires-python = ">=3.8"

[project.urls]
Homepage = "https://github.com/pwall2222/ValVault"
