# Include only packages necessary in development environment
name: base
channels:
  - defaults
dependencies:
  - ipykernel
  - pip
  - pip:
    - pep8-naming
    - flake8-isort
    - pytest
prefix: /opt/conda