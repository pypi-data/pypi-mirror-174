[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "DSBA_Final"
version = "0.0.8"
dependencies = [
                "numpy",
                "pandas",
]

[tool.setuptools]
include-package-data = true

[tool.setuptools.package-data]
DSBA_Final = ["*.csv"]