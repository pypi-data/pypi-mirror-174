[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "DSBA_Final"
version = "0.1.1"
dependencies = [
                "numpy",
                "pandas",
]
[options]
zip_safe = true
include_package_data = true

[options.package_data]
DSBA_Final = 'src/data/Final_Project_Raw_Data.csv'
