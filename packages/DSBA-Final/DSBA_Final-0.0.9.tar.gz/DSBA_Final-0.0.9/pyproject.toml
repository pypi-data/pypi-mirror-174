[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "DSBA_Final"
version = "0.0.9"
dependencies = [
                "numpy",
                "pandas",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
DSBA_Final = ["*.csv"]