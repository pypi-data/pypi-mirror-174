from setuptools import setup

setup(name='becloud',
      version='0.1.1',
      description='Backend Cloud Package',
      packages=['becloud'],
      install_requires=[],
      zip_safe=False,
      )
