from setuptools import setup

setup(name='fecloud',
      version='0.1.1',
      description='Backend Cloud Package',
      packages=['fecloud'],
      install_requires=[],
      zip_safe=False,
      )
