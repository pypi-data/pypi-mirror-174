R_PORT = "PORT"
R_PATH = "PATH"
R_PROCESS = "PROCESS"

# TODO: remove the use of environment variables for redis parameters
# TODO: For some reason there is always 2 tests that are being skipped because the parametrise gives
#  empty parameters
# TODO: Return the same response for each request, making every response in the same format
# TODO: Add build and publish CI on when a new tag is created
