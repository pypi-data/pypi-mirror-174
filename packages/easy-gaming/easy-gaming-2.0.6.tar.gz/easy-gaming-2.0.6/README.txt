This library can:
=================

autoChat module
---------------
- autoChat.chat(delay, speed)
- autoChat.spam(amount, speed, delay)
- autoChat.reset()

autoClicker module
------------------
- autoClicker.click('button', speed, duration, delay)

autoPos module
--------------
- record(delay, amount)
- play(speed, delay, move delay, 'button')