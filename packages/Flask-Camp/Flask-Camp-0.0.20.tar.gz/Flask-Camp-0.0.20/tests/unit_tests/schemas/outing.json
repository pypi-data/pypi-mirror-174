{
  "properties": {
    "data": {
      "if": {
        "properties": { "namespace": { "const": "outing" } },
        "required": ["namespace"]
      },
      "then": {
        "properties": {
          "value": { "type": "string" },
          "rating": { "$ref": "entities/rating.json" },
          "namespace": { "const": "outing" }
        },
        "required": ["value", "rating", "namespace"],
        "additionalProperties": false
      }
    }
  }
}
